#!/bin/bash

set -e

BASE_DIR=/opt/app-root/src
INSTALL_DIR=$BASE_DIR/bin
API_SPEC_DIR=$BASE_DIR/apispec
API_SPEC_FILE_DESTINATION="/opt/app-root/src/apispec/api.spec.file"

echo
echo "===> Build started at $(date)"
echo
START=$SECONDS

# Unconditionally print elapsed build time at exit
function finish {
  echo "===> Elapsed time: $(($SECONDS - $START)) seconds"
}
trap finish EXIT

SOURCE=/tmp/src

cd $SOURCE

echo "---> Building application source..."
make
make test


echo "---> Copying binaries into place..."
mkdir -p $INSTALL_DIR
cp gateway $INSTALL_DIR
cp job-receiver $INSTALL_DIR

echo "---> Copying openapi spec into place..."
pwd
find .
mkdir -p $API_SPEC_DIR
cp internal/controller/apispec/api.spec.json $API_SPEC_FILE_DESTINATION

echo
echo "===> Build completed at $(date)"
